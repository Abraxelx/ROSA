<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ROSA.Views.ItemsPage"
              Title="{Binding Title}"
             x:Name="BrowseItemsPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem  Clicked="AddItem_Clicked" Icon="settingsred.png">
        </ToolbarItem>
    </ContentPage.ToolbarItems>
    <ContentPage.Content>

        <StackLayout Padding="5, 25">
            <Label x:Name="TestLabel"
                Text="﷽" />
            

            <Button x:Name="Btn"
                    Text="CLICK"
                    Clicked="Btn_Clicked"
                    Pressed="Btn_Pressed"/>
        </StackLayout>
            
        
    </ContentPage.Content>
</ContentPage>